export const cmToIn = cm => {
  const inn = cm / 2.54
  return inn
}

export const inToCm = inn => {
  const cm = inn * 2.54
  return cm
}

export const cmToFbm = cm => {
  const fbm = cm / 2360
  return fbm
}